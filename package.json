{
  "name": "car-hub",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd backend && nodemon server.js\" \"cd frontend && npm start\"",
    "heroku-postbuild": "concurrently \"cd backend && node server.js\" \"cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^5.0.0"
  }
}
